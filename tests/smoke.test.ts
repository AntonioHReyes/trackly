import { describe, expect, it } from "vitest";

// Placeholder so `npm test` is green during scaffolding. Replaced by real
// domain/application specs starting Phase 1 (see PLAN.md).
describe("scaffolding", () => {
  it("test runner is wired up", () => {
    expect(true).toBe(true);
  });
});
